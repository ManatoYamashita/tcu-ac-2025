# 暗号化キー（64桁のHEX文字列）
# 生成: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NOTE: .env.local に実際のキーを設定してください
# ENCRYPTION_KEY=***

# ベースURL（本番環境では https://yourdomain.com に変更）
NEXT_PUBLIC_BASE_URL=http://localhost:3000
